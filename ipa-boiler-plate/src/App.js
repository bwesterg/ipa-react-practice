import './App.css';

// to run the json server run => json-server --watch db.json

//Trying to figure out github stuff, ensure files are merging.

// //I don't understand what is happening.  Keep getting message "error: Your local changes to the following files would be overwritten by checkout:
//         .DS_Store
//         Please commit your changes or stash them before you switch branches.
//         Aborting"

function App() {
  return (
    <div className="App">
      <h1>IPA APP</h1>
      <h2>Who needs API's when you have IPA's</h2>
    </div>
  );
}

export default App;
